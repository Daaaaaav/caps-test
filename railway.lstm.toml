[build]
dockerfilePath = "Dockerfile"
dockerBuildTarget = "lstm-service"

[deploy]
startCommand = "uvicorn LSTM_Service:app --host 0.0.0.0 --port 8001"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
